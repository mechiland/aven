# Typography: round 04 capture plan

Status: executed. All 16 current Aven frames were captured, and all 32 current/stock originals were inspected. Current runtime-scale checks passed. See [round 04 findings](typography-round04-findings.md). The procedure below records the plan used after root's explicit handover; no stock controls or font/profile/style changes were made.

The [machine-readable plan](../typography/round04-capture-plan.json) maps all 16 captures to the exact canonical stock image and hash. The historical [round 02 manifest](../typography/aven-matrix.json) and [geometry](../typography/aven-matrix-geometry.json) remain preserved. Results are recorded separately in the [round 04 manifest](../typography/aven-round04-matrix.json), [geometry](../typography/aven-round04-matrix-geometry.json), and [findings](typography-round04-findings.md).

## Matched conditions

Use the unchanged [stock protocol](typography-stock-matrix.md), with these concrete conditions:

| Scale | Physical output | Logical output | Requested outer window frame |
|---:|---:|---:|---|
| 1 | 1280 × 720 | 1280 × 720 | x=40, y=28, width=1200, height=630 |
| 1.25 | 1600 × 900 | 1280 × 720 | Same |
| 1.5 | 1920 × 1080 | 1280 × 720 | Same |
| 2 | 2560 × 1440 | 1280 × 720 | Same |

Inspect the actual `kscreen-doctor -o` output before using mode IDs. Add only missing test modes through its display API, then set physical mode and compositor scale together. Record acknowledged outer and client geometry; preserve real decorations and fractional rounding instead of resampling pixels.

The fixture must retain SHA-256 `ff6f9644427439f80bd3b9f0825ec9542ed2d41dfcddb28b9fe54a6f68c9a6b5`. The same mixed-file directory has 12 files at `$HOME/Documents/日常 · Everyday`. Use one Dolphin tab, native detail view (`Ctrl+3`), ascending name order, at the top. Preserve each integration's thumbnail and sidebar defaults.

For each locale, close existing Dolphin and Firefox processes first, including a leftover Dolphin daemon if it would inherit the wrong locale. Launch both with explicit matching `LC_ALL`, `LANG`, and `LANGUAGE`, because SSH forwards a C locale. Verify process environments and native localized Dolphin labels. Use `zh_CN.UTF-8` / `zh_CN` for SC and `zh_TW.UTF-8` / `zh_TW` for TC.

Firefox must use `~/.local/bin/aven-browser` and its existing Aven profile, one tab, page zoom 100% (`Ctrl+0`). Navigate the active tab explicitly to `file:///home/aven/aven/fixtures/typography/specimen.html#sc` or `#tc`. Browser process locale alone does not establish full browser UI localization.

## Capture sequence

1. After handover, inventory visible windows, display modes and processes; verify the fixture hash. Wake an idle display through native input and inspect a temporary frame before retaining evidence.
2. Launch SC Dolphin and Firefox. For scales 1, 1.25, 1.5 and 2, arrange Firefox then Dolphin to the matched frame. Capture native SC UI; activate and arrange Firefox, then capture SC prose. Save the acknowledged geometry for each retained frame.
3. Close both apps through their native UI, handle any quit confirmation, and verify they exited. Repeat the four scales with TC process locales and `#tc` prose.
4. Capture immutable original PNGs and sidecars with `scripts/capture.py`, guest `aven`, round `4`, scenes `typography-ui-sc-matrix`, `typography-prose-sc-matrix`, `typography-ui-tc-matrix`, and `typography-prose-tc-matrix`. The scale suffix is generated by the capture tool. If a frame is wrong, retain and exclude it; use a distinct replacement scene.
5. Validate every sidecar against actual scale. Integer scales use QScreen/KScreen agreement. Fractional scales require the post-capture mapped QWindow DPR to agree with KScreen; QScreen's rounded 2 is recorded honestly. Check the submitted native buffer and automatic restoration of the prior KWin focus UUID. Never rewrite sidecars to make a check pass.
6. Open every actual PNG at original pixels. Record hashes and specific observations in the new matrix manifest; pair each with the plan's canonical stock image. Inspect native Chinese labels, mixed Latin/numbers, punctuation, dense glyphs, wrapping, clipping and fractional softness. Do not infer new visual gains from unchanged font settings or an old screenshot.
7. Restore 1920 × 1200 at scale 1, close apps, verify window/process state, and hand the guest back to root. Root's final handoff refinement was followed: before closing, restore both apps to normal outer frames (240,100,1440,900), so the exported VM does not retain matrix-sized windows. Complete the separate round 04 findings report without assigning critic scores.

## Evidence limits

The requested frames have matching outer geometry, not identical client areas. Stock and Aven row density and thumbnail defaults differ. The prose fixture uses identical authored 17 px / 1.75 CSS, so its spacing is not an Aven-only improvement. Current mail compose leading is covered by root's separate primary scenes, including its wrapped-paragraph check; this matrix does not itself test mail or motion.

Current review context: the independent [round 03 critic](critic-round-03.md) reported Chinese 8.4 and overall 8.29, with no final pass. The [compose rhythm supplement](compose-rhythm-review.md) confirms a later visible leading correction without revising those scores. Only the new independent review can determine the outcome of round 04.
